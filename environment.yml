name: qfz
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch>=2.0
      - torchvision>=0.15
      - pennylane>=0.35
      - numpy>=1.24
      - scikit-learn>=1.3
      - matplotlib>=3.7
      - tqdm>=4.65
      - pytest>=7.0
